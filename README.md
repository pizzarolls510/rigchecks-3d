# RigCheck 3D

A mobile-first, local-only GLB animation inspector. The application is a static PWA contained entirely in `dist/`.

## GitHub Pages

Create a repository, push this project, then enable **Settings → Pages → GitHub Actions**. The included workflow publishes `dist/`.

Models opened in the viewer stay on the device. A self-contained `.glb` is the recommended format.
